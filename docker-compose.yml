version: "3.9"
services:
  mysql:
    image: mysql:8.0
    container_name: masinsatisi-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: masinsatisi
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
